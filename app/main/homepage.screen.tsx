import React, {useState} from 'react';
import { Text, ImageBackground, SafeAreaView, ScrollView, View, Alert} from "react-native";

export const Homepage = () => {
    return (
        <Text>Homepage</Text>
    )
}